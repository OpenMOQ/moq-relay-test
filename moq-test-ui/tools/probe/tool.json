{
  "name": "probe",
  "displayName": "Relay Probe",
  "description": "Probes relay endpoints for liveness, supported drafts, transport types, and latency.",
  "category": "diagnostics",
  "order": 2,
  "docker": {
    "image": "ghcr.io/gmarzot/aiomoqt:0.10.6",
    "entrypoint": ["/bin/sh", "-c"],
    "command": ["python /app/aiomoqt/examples/relay_probe.py -f /tmp/relays.json -o /tmp/probe_out.json {build_args} && cat /tmp/probe_out.json && echo"],
    "network": "host"
  },
  "prepareFiles": [
    {
      "containerPath": "/tmp/relays.json",
      "generator": "relay-probe-endpoints"
    }
  ],
  "parameters": [
    {
      "id": "relay_url",
      "label": "Relay URL",
      "type": "url",
      "required": true,
      "default": "",
      "placeholder": "https://relay.example.com/moq-relay",
      "description": "MoQT relay endpoint to probe"
    },
    {
      "id": "transport",
      "label": "Transport",
      "type": "select",
      "default": "both",
      "options": [
        { "value": "both", "label": "QUIC + WebTransport (H3)" },
        { "value": "quic", "label": "QUIC" },
        { "value": "wt",   "label": "WebTransport (H3)" }
      ]
    }
  ],
  "buildCommand": "",
  "filters": [
    { "id": "debug", "label": "Hide debug", "pattern": "^\\[DEBUG\\]|^D\\d{4}", "default": true }
  ],
  "renderer": "renderer.js",
  "selfTest": {
    "enabled": true,
    "order": 2,
    "selfTestTransport": "all",
    "defaults": {
      "transport": "both"
    }
  }
}
